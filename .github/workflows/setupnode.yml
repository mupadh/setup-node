name: setup nodejs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

runs-on: ubuntu-latest

jobs: 
 build:
 
steps:
- uses: actions/checkout@v2
- uses: actions/setup-node@v2
  with:
   node-version: '14'
   
-run: npm install
-run: npm test
